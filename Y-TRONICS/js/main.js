// MAIN INITIALIZATION - This is the entry point
document.addEventListener('DOMContentLoaded', () => {
    // Load products first
    loadProducts();
    
    // Initialize all components
    initFilters();
    initUI();
    
    console.log('Y-TRONICS website loaded successfully! 🚀');
});
